# Brew

**Brew** is a small React coffee shop menu app built using components, props, and JSX. It demonstrates creating and reusing components, rendering lists, and using conditional rendering to display different UI states.
